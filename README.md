# SAT Landings

Frontend para landings, campañas y cotizadores de Seguro a Tiempo.

## Desarrollo

```bash
cp .env.example .env
npm install
npm run dev
```

La URL de la API se configura con `VITE_API_URL`. Las variables `VITE_*` son públicas y nunca deben contener secretos.
